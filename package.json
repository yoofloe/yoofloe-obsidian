{
  "name": "obsidian-yoofloe",
  "version": "0.1.1",
  "description": "Read-only Yoofloe reports inside Obsidian.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "esbuild": "^0.25.2",
    "obsidian": "^1.8.10",
    "typescript": "^5.8.3"
  }
}
